#!/usr/bin/env bash
# First-time developer setup — convenience only, never CI-critical.
# TODO:
#   1. python -m venv .venv && pip install -e ".[dev]"
#   2. databricks auth login --host <workspace>
#   3. databricks bundle deploy --target dev
#   4. databricks bundle run source_emulator --target dev -- --scenario day1_clean
#   5. databricks bundle run orchestrator --target dev
set -euo pipefail
echo "TODO: see comments"
