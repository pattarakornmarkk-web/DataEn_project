"""Integration assertion definitions — the library home of the deployed-pipeline checks.

Executed in-workspace by the integration-assertions entry point (job.integration_tests);
tests/integration/ contains thin pytest wrappers over these same definitions.
Mirrors the audit.invariants pattern: logic here, runners thin.
"""
