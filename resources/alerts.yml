# SQL alerts on ops tables — v1.1 roadmap item (see CHANGELOG / README limitations).
# Planned: quarantine_rate_breach, expected_data_missing, run_failed,
# freshness_sla_breach, audit_absence_canary (R4), held_orphans_present (ADR-0006).
resources: {}
