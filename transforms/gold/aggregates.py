# GOLD | sales_per_region_daily, sales_per_store_daily, funnel_conversion_daily.
# Generated via the closure-safe factory (transform.aggregates.make_segment_aggregate).
# Conservation contract: sum(aggregate) == sum(fct_sales revenue), exactly.
#
# TODO: @dp.materialized_view declarations (factory-generated where per-segment)
