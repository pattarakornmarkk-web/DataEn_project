# BRONZE | customer_updates_raw — CDC feed, TWO emitters (crm/, mobile_app/).
# Structure: dp.create_streaming_table + two @dp.append_flow (from_crm, from_mobile_app).
#
# TODO: streaming table + two append_flow declarations
